# Requirements Traceability Matrix (07_REQUIREMENTS_TRACEABILITY.md)

This matrix maps stable requirement identifiers to design documents, status indicators, and planned testing structures.

---

## Traceability Matrix

| Requirement ID | Summary | Source | Decision Status | Implementation Status | Planned Module | Planned Tests | Milestone | Notes |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **PROD-001** | Selection of exam triggers rules | PDF Sec 1.1 | CONFIRMED | Documentation Complete | `apps/web`, `packages/exam-rules` | Search route assertions | Milestone 2 | First step in workflow |
| **CROP-001** | Crop Mode A (Exact Size) | PDF Sec 5.3 | CONFIRMED | **Implemented (Milestone 9)** | `services/image-engine` | `test_crop_mode_a.py`, `test_portrait_composition.py` | Milestone 9 | Aspect ratio preserved; exact outputs prefer white-background padding over subject clipping and use semantic portrait composition instead of full foreground bounds for tight framing. |
| **CROP-002** | Crop Mode B (Ranges/Unspecified) | PDF Sec 5.3 | CONFIRMED | **Implemented (Milestone 10)** | `services/image-engine` | `test_crop_mode_b.py`, `test_rule_pipeline.py`, `test_portrait_composition.py` | Milestone 10 | Range and unspecified dimension rules use tight head-led portrait composition with documented platform defaults where needed. |
| **BG-001** | Background cleaning | PDF Sec 5.2 | CONFIRMED | **Implemented (Milestone 11)** | `services/image-engine/providers` | `test_background_composition.py` | Milestone 11 | Solid background composition |
| **SEG-001** | Portrait coarse segmentation | PDF Sec 5.2 | CONFIRMED | **Implemented (Milestone 7)** | `services/image-engine` | `test_subject_segmentation.py` | Milestone 7 | MediaPipe multiclass & binary segmenters |
| **SEG-002** | Coarse-mask edge refinement and trimap | PDF Sec 5.2 | CONFIRMED | **Implemented (Milestone 8)** | `services/image-engine` | `test_foreground_refinement.py` | Milestone 8 | NumPy & PIL morphological operations |
| **ID-001** | Identity preservation | PDF Sec 5.5 | CONFIRMED | **Partially Implemented (Milestone 12)** | `services/image-engine` | `test_deterministic_output_preparer.py` | Milestone 12 | Restrained global enhancement baseline implemented; full identity-preservation validation remains ongoing through final validation and QA. |
| **RULE-001** | Versioned structured rules | PDF Sec 6 | CONFIRMED | **Implemented (Milestone 2)** | `packages/exam-rules`, `services/image-engine` | `test_rule_validation.py` | Milestone 2 | JSON schema & Python models |
| **FILE-001** | Target output naming | PDF Sec 5.7 | CONFIRMED | **Implemented (Milestone 14)** | `services/image-engine` | Pattern check tests | Milestone 14 | Sanitize string output |
| **COMP-001** | Quality-aware compression | PDF Sec 5.6 | CONFIRMED | **Implemented (Milestone 13)** | `services/image-engine` | `test_image_compression.py` | Milestone 13 | Below maximum ceiling |
| **DPI-001** | Target output DPI/JFIF density | User benchmark config | CONFIRMED | **Implemented** | `services/image-engine`, `packages/exam-rules` | `test_image_compression.py`, `test_rule_pipeline.py`, `test_rule_validation.py` | Benchmark Rules | Optional rule-level `dimensions.dpi` is resolved into JPEG compression and final validation. |
| **PRIV-001** | Deletion lifecycle | PDF Sec 10.2 | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/api` | `test_api.py`, `tests/api/test_retention_window.py` (11 tests), `test_service_hardening.py` sweeper suite | Milestone 15; DEC-066 | Manual deletion, a sweeper that actually runs (DEC-064), and a bounded window: **thirty minutes, enforced at the deadline rather than at the next sweep** (DEC-066). Expiry was previously consulted only by the timer, so an expired -- and paid -- job returned 200 with the clean file for up to five minutes past its deadline. `is_expired()` is now checked on all six job routes, which erase the job on the way to refusing it, and expired jobs leave their kit. A deadline that cannot be parsed reads as expired. **DEC-067** adds the candidate's own postponement: `POST /v1/jobs/{id}/extend` grants one more window bounded at one hour from creation -- what every file used to get unconditionally -- so the option cannot lengthen the worst case, and it never releases a gated job. The matching **warning before the deadline is the UI lane's and outstanding**; the extension is inert without it, because an expired job cannot be extended. |
| **SEC-001** | Magic byte validation | PDF Sec 10.4 | CONFIRMED | **Implemented (Milestone 3)** | `services/image-engine` | `test_signatures.py` | Milestone 3 | Input signature sanitization |
| **QA-001** | Visual regression testing | PDF Sec 12.3 | CONFIRMED | **Implemented (Milestone 5)** | `QA Strategy` | Visual checks runs | Milestone 5 | Golden images setup |
| **UX-001** | Mobile responsiveness | PDF Sec 11.5 | CONFIRMED | **Partially Implemented (Milestone 16)** | `apps/web` | CSS media queries / responsive grid assertions | Milestone 16 | Mobile-first grid layouts |
| **SUIT-001** | Suitability framework & provider interfaces | PDF Sec 5.2 | CONFIRMED | **Implemented (Milestone 4)** | `services/image-engine/suitability` | Evaluator, Quality metrics, and Provider contracts tests | Milestone 4 | Framework structure & contracts |
| **FACE-001** | Face detection integration | PDF Sec 5.2 | CONFIRMED | **Implemented (Milestone 5)** | `services/image-engine/providers` | `test_mediapipe_face_detector.py` | Milestone 5 | MediaPipe face detection integration |
| **HEAD-001** | Provisional geometric head-box estimator | PDF Sec 5.2 | CONFIRMED | **Implemented (Milestone 6)** | `services/image-engine/providers` | `test_landmark_geometric_head_estimator.py` | Milestone 6 | Heuristic-driven head estimator |
| **HEAD-002** | Semantic exam portrait composition | PDF Sec 5.2/5.3 | CONFIRMED | **Implemented (Milestone 18)** | `services/image-engine/providers` | `test_portrait_composition.py` | Milestone 18 | Decouples crop-framing geometry from full foreground segmentation so torso/shoulder masks do not drive tight exam crops. |
| **API-001** | Local API processing boundaries | PDF Sec 8 | CONFIRMED | **Implemented (Milestone 15)** | `services/image-engine/src/exam_photo/api` | `test_api.py` | Milestone 15 | Synchronous endpoint execution |
| **STORAGE-001** | Local temporary artifact storage | PDF Sec 8.2 | CONFIRMED | **Implemented (Milestone 15)** | `services/image-engine/src/exam_photo/api` | Traversal check tests | Milestone 15 | Strict boundary guards |
| **WEB-001** | Public Web App MVP | PDF Sec 11.1 | CONFIRMED | **Implemented (Milestone 16)** | `apps/web` | `api-client.test.ts`, `file-validation.test.ts` | Milestone 16 | React Next.js single-page application MVP |
| **PRIV-UX-001** | Privacy-First User Flow | PDF Sec 10.2 | CONFIRMED | **Implemented (Milestone 16)** | `apps/web` | `delete-flow.test.tsx`, `validation-report.test.tsx` | Milestone 16 | No localStorage caching, URL revocation, manual deletion |
| **ADMIN-001** | Local admin configuration console | PDF Sec 11.2 | CONFIRMED | **Implemented (Milestone 17)** | `apps/web` | `rule-admin.test.tsx` | Milestone 17 | Local-only accidental-exposure warning gated admin page |
| **RULE-UI-001** | Rule editor with form/json and reset views | PDF Sec 11.2 | CONFIRMED | **Implemented (Milestone 17)** | `apps/web` | `rule-admin.test.tsx` | Milestone 17 | Advanced field preservation and reset behavior |
| **RULE-VALIDATION-API-001** | Stateless backend rule validation API | PDF Sec 8.3 | CONFIRMED | **Implemented (Milestone 17)** | `services/image-engine` | `test_rule_validation_api.py` | Milestone 17 | Returns 422 for bad bodies, does not write files |
| **MAT-001** | Guided Filter soft-alpha matting | PDF Sec 5.2 | CONFIRMED | **Implemented (Milestone 18)** | `services/image-engine/providers/refiners` | `test_matting_and_decontamination.py` | Milestone 18 | Two-resolution Guided Filter edge refinement |
| **DECON-001** | Edge color decontamination | PDF Sec 5.5 | CONFIRMED | **Implemented (Milestone 18)** | `services/image-engine/providers` | `test_matting_and_decontamination.py` | Milestone 18 | Restricted to boundary region ($0.05 < \alpha < 0.95$) |
| **COMPOS-002** | Premultiplied-alpha composite | PDF Sec 5.2 | CONFIRMED | **Implemented (Milestone 18)** | `services/image-engine/providers` | `test_matting_and_decontamination.py` | Milestone 18 | Prevents dark/bright edge fringes during resize |
| **BENCH-001** | Visual quality & composition benchmark | PDF Sec 12.3 | CONFIRMED | **Implemented (Milestone 18)** | `scripts`, `tests/fixtures/engine_quality` | `benchmark_engine_quality.py` | Milestone 18 | 30 base subjects, 192 variants, visual metrics |
| **CROP-003** | Anatomical head-span as composition reference | User benchmark set (60 reference photos) | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/providers/crop_planners`, `orchestration/rule_resolver.py` | `test_crop_mode_a.py`, `test_rule_pipeline.py` | Post-Milestone 18 | DEC-029: composition ratios measured against crown-to-chin span, not the wider preservation box; per-photo strict/relaxed candidate search replaces profile-level hair-clipping rules. |
| **MATTE-002** | Matte resolution, band width, and alpha contrast window retuned to reference set | User benchmark set (60 reference photos) | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/providers/refiners/morphological_refiner.py`, `providers/foreground_decontamination.py` | `test_foreground_refinement.py`, `test_matting_and_decontamination.py` | Post-Milestone 18 | DEC-030: working resolution derived from the pixel budget instead of dropping a quality tier; matting band and colour-decontamination reach recalibrated together. |
| **MATTE-003** | BiRefNet subject matting backend (default) | User comparison of MediaPipe, RMBG-2.0, BiRefNet, BEN2 | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/providers/segmenters/birefnet_segmenter.py`, `orchestration/rule_pipeline.py`, `cli.py`, `scripts/download_birefnet.py`, `model-manifests/birefnet.json` | `test_matting_and_decontamination.py`, reference-pair benchmark | Post-Milestone 18 | DEC-031/036: MIT-licensed BiRefNet selected on measured edge quality and now defaulted for pipeline and CLI callers. MediaPipe remains selectable for diagnostics. |
| **BENCH-002** | Matched 60-photo engine-vs-ideal reference benchmark | User reference output folders | CONFIRMED | **Implemented** | `scripts/benchmark_reference_pairs.py` | Numeric JSON/CSV benchmark artifacts outside version control | Post-Milestone 18 | DEC-036: compares each engine output against the matching ideal output for crop dimensions, side/head/chin margins, 75% floor count, no-output keys, and regional edge behavior at hair, ears, beard line, chin, and neck. |
| **CROP-004** | Composition margins calibrated to the approved ideal outputs | User reference output set (60 approved photos) | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/providers/crop_planners/deterministic_crop_planner.py` | `test_crop_mode_a.py`, reference-pair benchmark | Post-Milestone 18 | DEC-037: mandatory below-chin and above-hair margins read from the measured ideal distribution at its p25 instead of feasibility algebra, which had capped head height at 0.758 for every photo. |
| **CROP-005** | Planner and validator judge one shared subject region | Engine defect analysis | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/providers/crop_planners/deterministic_crop_planner.py` | `test_rule_pipeline.py::test_pipeline_integration_success` | Post-Milestone 18 | DEC-038: face containment anchored on the mandatory head box plus located landmarks, under the same `allow_subject_clipping` condition the candidate search uses, removing a planner/validator disagreement in both directions. |
| **CROP-006** | Minimum face coverage relaxed last rather than never | User reference output set | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/providers/crop_planners/deterministic_crop_planner.py` | `test_crop_mode_a.py` | Post-Milestone 18 | DEC-039: an unwaivable floor routed impossible photos to a constraint-free fallback that scored worse on the floor itself; it is now the final relaxation tier and the compromise is reported. |
| **MATTE-004** | Matte computed on the crop region at native resolution | Reviewer edge-quality report; 60-photo measurement | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/orchestration/rule_pipeline.py` | Reference-pair benchmark, spot-check comparison | Post-Milestone 18 | DEC-040: a fixed 512px model budget spent on the whole frame left the head with a median 121 px of alpha detail before up to 17.4x magnification; the matte is now recomputed on the planned crop region. |
| **SUIT-002** | Accept / warn / block output disposition policy | Product decision; 48-exam rule research | CONFIRMED | **Specified (implementation pending)** | `services/image-engine/src/exam_photo/suitability`, `apps/web` | Pending | Post-Milestone 18 | DEC-041: block only when a truthful output is impossible (undecodable file, no face, multiple faces); every detectable appearance defect produces output plus a `likely_rejection` or `possible_issue` warning; unverifiable rules are guidance only. |
| **RULE-005** | Per-exam appearance rules with three-state policies | 48-exam official-source research | CONFIRMED | **Specified (implementation pending)** | `packages/exam-rules/schema/exam-rule.schema.json`, `services/image-engine/src/exam_photo/models/exam_rule.py` | Pending | Post-Milestone 18 | DEC-042: spectacles, headwear, background colour, face coverage, smile, colour-vs-monochrome, ears and the name/date imprint are carried per exam; `permitted`/`prohibited`/`conditional` because bodies contradict each other. |
| **ENH-001** | Natural enhancement corrects the capture, never the subject | Product owner request; 40-photo adversarial set | CONFIRMED | **Partially Implemented** | `services/image-engine/src/exam_photo/suitability/enhancement_planner.py` | `test_enhancement_planner.py` | Post-Milestone 18 | DEC-043: tonal-range stretch, clipping recovery, partial illuminant neutralisation and bounded sharpening, each triggered only by its own measured deficit and capped conservatively. A correctly exposed dark-skinned face is left untouched by construction; noise reduction is specified but not yet implemented. |
| **CROP-007** | Below-chin space bounded in the search, not left as a residual | Product owner framing rule; 60 approved ideal outputs; reviewer verdicts | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/providers/crop_planners/deterministic_crop_planner.py` | `test_output_invariants.py`, `test_crop_mode_a.py::test_crop_complete_head_containment`, `test_visual_regression.py` | Post-Milestone 18 | DEC-044: the bottom edge is anchored to the chin plus a small beard margin and ranked above the eye line in the relaxation ladder, so framing wins and the eye line lands where the frame puts it; below-chin space had been whatever head height and top margin left over. Invariant sweep 224 -> 0 of 960 geometries. |
| **RULE-006** | 39 examinations encoded from researched evidence by a repeatable converter | 48-examination official-source research | CONFIRMED | **Implemented** | `scripts/encode_exam_rules.py`, `examples/rules/exam_*.json`, `packages/exam-rules/research/` | End-to-end run across five rules spanning every dimension mode | Milestone 19 | Rule records are generated, never hand-written, so re-running reproduces the catalogue and no record can drift from its evidence. 17 verified, 15 verified_with_ambiguity, 7 provisional; 11 not encoded and listed in `docs/EXAM_RULE_GAP_REGISTER.md`. |
| **CROP-008** | One composition for every exam; dimensions chosen from the photograph | Product owner ruling; 48-examination research | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/orchestration/rule_resolver.py`, `orchestration/rule_pipeline.py` | `test_rule_pipeline.py::test_rule_resolver_unspecified_dimensions_sizes_from_the_photograph`, `::test_rule_resolver_honours_a_published_preferred_size` | Milestone 19 | DEC-045: the loose crop presets are removed, a published preferred size is honoured instead of a platform default, and composition shortfalls warn instead of blocking. The engine refuses rather than distorts when an aspect cannot be met. |
| **RULE-007** | Landscape pixel figures recorded upright | Self-contradictory published sources | CONFIRMED | **Implemented** | `scripts/encode_exam_rules.py` | End-to-end run on RRB Level-1 and RRB NTPC | Milestone 19 | DEC-046: RRB publishes "35mmX45mm or 320 x 240 pixels", whose magnitudes agree but whose orientations do not. Recorded upright with the published figure preserved and the transposition stated in provenance. |
| **RULE-008** | Examination rule carries the full deliverable inventory | 48-examination deliverables research; product-direction report | CONFIRMED | **Implemented** | `packages/exam-rules/schema/exam-rule.schema.json`, `services/image-engine/src/exam_photo/models/exam_rule.py` | `test_exam_requirements.py` | Phase A | DEC-047: `requirements[]` records every item an exam asks for, including the ones the platform cannot produce. A method the platform cannot execute cannot be marked supported, and `supported` on a non-photograph item requires a `file_spec` behind it. The photograph requirement carries no `file_spec`; its specification remains `image_requirements`. |
| **RULE-009** | Interim placeholder values are distinguishable from evidence | Product owner direction on missing signature specifications | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/models/provenance.py`, `services/image-engine/src/exam_photo/models/exam_rule.py` | `test_exam_requirements.py` | Phase A | DEC-048: `interim_default` provenance requires reasoning, is pinned to confidence 1, cannot be approved, and blocks verified status for the whole rule. The signature interim is the modal published spec (10-20 KB JPEG), not the mean, which would exceed five of the six published ceilings. |
| **RULE-010** | Deliverable inventory generated from versioned research | 48-examination deliverables research | CONFIRMED | **Implemented** | `scripts/extract_deliverables.py`, `scripts/encode_exam_rules.py`, `examples/rules/exam_*.json` | `test_exam_requirements.py` catalogue guards | Phase A | DEC-049: extraction and interpretation are separate scripts so a judgement call cannot be recorded as a reading of the source. All 39 encoded exams carry an inventory; every non-photograph deliverable is `not_yet_supported` until the engine that prepares it exists. |
| **INK-001** | Ink-on-paper deliverables corrected aggressively, geometry untouched | Product owner requirement; 5-image signature/thumb reference set | CONFIRMED | **Implemented (framing partial)** | `services/image-engine/src/exam_photo/ink/` | `tests/ink/test_ink_preparation.py` | Phase B | DEC-050: paper driven to white and ink to full strength, opposite to DEC-043's restraint on faces, with stroke shape never altered. Show-through and the hand holding the sheet are rejected. Framing on a small sheet in a large frame remains loose and is held by a ratcheted test. |
| **INK-002** | Ink path holds across unseen capture conditions | Product owner concern that constants were tuned to five photographs | CONFIRMED | **Implemented** | `services/image-engine/tests/ink/test_ink_robustness.py`, `.github/workflows/image-engine-ci.yml` | ~100-case synthetic sweep, own CI stage | Phase B | DEC-050: structural assertions over ink colour, exposure, colour temperature, uneven light, paper tone, distance, focus, noise and show-through. Caught a declaration losing its lower lines and proximity grouping running twice at different scales. |
| **PDF-001** | PDF deliverables assembled and prepared without re-rendering documents | Product owner direction; certificate requirements across 39 exams | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/pdf/` | `tests/pdf/test_pdf.py` | Phase B | DEC-052: a scan may be re-encoded and downscaled; a digitally issued document may not be rasterised to hit a byte ceiling. Pages classified by image area and extractable text. 135 of 155 requirements now served. |
| **PDF-002** | Candidate adds pages to a document and arranges them | Product owner request | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/pdf/document.py` | `tests/pdf/test_pdf_document.py` | Phase B | DEC-053: uploads flatten into pages with stable identities; order, rotation, omission and repetition are the caller's to choose. A photographed page may be re-encoded to meet a ceiling; a page whose text is real text never is. |
| **API-002** | Exam catalogue is reachable over the API | Pivot direction; DEC-047 inventory | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/orchestration/rule_catalogue.py`, `api/app.py`, `api/settings.py` | `test_rule_catalogue.py`, `test_exam_catalogue.py` | Phase C | DEC-055: nothing under `services/image-engine/src` read `examples/rules/`, so 39 examinations and 155 requirements were invisible to every caller. `GET /v1/exams` and `GET /v1/exams/{exam_id}`. A bad record costs only its own examination; a duplicate `exam_id` withdraws every claimant rather than last-one-wins. |
| **API-003** | One preparation endpoint per requirement, dispatching by type | Exam First, Tool Second (AGENTS.md §2.1) | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/api/app.py`, `api/service.py`, `orchestration/deliverable_pipeline.py`, `orchestration/rule_pipeline.py` | `test_kit_endpoints.py` | Phase C | DEC-055: `POST /v1/exams/{exam_id}/requirements/{requirement_id}/prepare`. The caller names an item of an examination, never a pipeline; dispatch on `requirement_type` sits beside `_TREATMENT_BY_TYPE` rather than being duplicated in the browser. `/v1/process` retained unchanged for the rule-admin console. |
| **API-004** | Platform-support gate refuses at the API boundary | AGENTS.md §5 no fictional successes; DEC-047 | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/api/app.py`, `api/service.py`, `api/contracts.py` | `test_kit_endpoints.py` gate suite | Phase C | DEC-056: the model refuses to record an impossible support state, the API refuses to act on one -- two independent barriers. 409 carries the requirement's own vocabulary, runs before the upload is read, and creates no job. All five `platform_support` values pass through; no response field reduces them to a boolean. |
| **API-005** | Document plan-and-assemble endpoint pair | DEC-053 page identities | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/api/app.py`, `api/service.py`, `pdf/document.py` | `test_kit_endpoints.py` document suite | Phase C | DEC-055: two calls because a single call offers no point at which the candidate arranges anything. Uploads outlive the first request inside the existing job directory, stored under generated names rather than the candidate's own. Order, omission and rotation honoured; an unreadable upload costs only itself. |
| **API-006** | Kit package: files, checklist and validation report | Product-direction report | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/api/service.py`, `api/app.py`, `api/jobs.py` | `test_kit_endpoints.py` package suite | Phase C | DEC-055 step 4, DEC-057: the report names every `interim_default` value explicitly. The checklist lists every requirement including refused ones. Nothing carrying a non-served support state is counted as an output, and a deleted job leaves the kit. |
| **API-007** | Produced files are served with their own media type | DEC-052 PDF deliverables | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/api/app.py`, `api/service.py`, `api/jobs.py` | `test_kit_endpoints.py::test_the_prepared_file_is_downloadable_with_its_own_media_type`, `::test_assembly_honours_the_candidates_order_and_omissions` | Phase C | `/v1/jobs/{job_id}/output` hardcoded `image/jpeg`, which was correct while a photograph was the only output and wrong once a deliverable could be a PDF. The type is recorded on the job and unknown extensions fall back to `application/octet-stream` rather than to an image guess. |
| **KIT-001** | The inventory is the home screen, not a wizard step | Pivot direction; DEC-047 | CONFIRMED | **Implemented (read side)** | `apps/web/src/app/exam/[examId]/page.tsx`, `components/exam/requirement-card.tsx`, `lib/catalogue.server.ts` | `exam-page-logic.test.ts`, `exam-search.test.tsx`; 39 pages build | Phase C | DEC-056: 2-8 items completed in arbitrary order across sittings, replacing the linear pick-rule/upload/download form. `upload-card`, `result-preview`, `validation-report` and `processing-status` are recomposed; `rule-selector` is replaced by an exam picker. |
| **KIT-002** | Unavailable requirements and unencoded examinations stay visible | Product owner ruling | CONFIRMED | **Implemented** | `scripts/encode_exam_rules.py`, `services/image-engine/src/exam_photo/orchestration/rule_catalogue.py`, `api/app.py`, `apps/web/src/components/` | `test_rule_catalogue.py`, `test_exam_catalogue.py`; rendering pending | Phase C | DEC-056: an absence the candidate discovers at the portal is worse than one admitted here. The data half is done -- the encoder emits `examples/rules/unavailable_examinations.json` and `GET /v1/exams` returns all 11 unencoded examinations with their reason and deliverable count, kept in a separate list from the 39 selectable ones. 16 guidance-only requirements; 10 of the 11 carry 23 non-photograph deliverables between them, all four SSC among them. Rendering is pending. |
| **KIT-003** | Kit state held in the browser, artifacts under the server TTL | Privacy posture; DPDP Act | CONFIRMED | **Partially Implemented** | `apps/web/src/lib/`, `services/image-engine/src/exam_photo/api/jobs.py` | `test_kit_endpoints.py` package suite; browser half pending | Phase C | DEC-058: the server half is done -- `kit_id` is a grouping key on the job manifest and `jobs_in_kit` gathers a kit from manifests the registry already scans, adding no retention surface. The browser half is pending. **Closed by DEC-066**: `job_ttl_seconds` is 1800, so a multi-sitting kit is bounded by the artifact lifetime at thirty minutes rather than an hour, and an expired job leaves the kit so a package cannot claim a deliverable that is gone. |
| **WEB-002** | Produced-with-caveats is a first-class outcome state | DEC-041 produce, never refuse | CONFIRMED | **Implemented** | `apps/web/src/components/exam/outcome-result.tsx`, `requirement-upload.tsx` | `requirement-upload.test.tsx` (16 tests) | Phase C | DEC-056: `is_blank`, `findings`, `ceiling_was_unpublished`, `exceeds_ceiling` and DEC-051's unreachable-minimum message are reported rather than raised. A two-state interface files them under success, where nobody reads them. |
| **WEB-003** | Exam pages are statically generated and indexable | Pricing strategy §16, §27: SEO is the primary distribution channel | CONFIRMED | **Implemented** | `apps/web/src/lib/catalogue.server.ts`, `apps/web/src/app/exam/[examId]/page.tsx` | 39 prerendered pages in the build; rendered HTML asserted to carry real catalogue data | Phase C | The catalogue is read from `examples/rules/` at build time rather than fetched from the API in the browser. A page whose content arrives by client-side fetch is an empty document to a crawler, which would forfeit the acquisition strategy at Rs 4/Rs 8 where paid CAC must stay under ~Rs 1.50. Also makes search work with the processing service down. |
| **WEB-004** | Photograph rules answered from the exam's own record | Candidate questions: spectacles, headwear, expression | CONFIRMED | **Implemented** | `apps/web/src/lib/appearance-rules.ts`, `components/exam/guidance-list.tsx` | `exam-page-logic.test.ts` guidance suite | Phase C | Nothing generic: where the record is silent the page says nothing rather than inventing a rule. Conditional headwear keeps its condition in full, since several bodies permit religious coverings that others prohibit and flattening that would be wrong for those exams. The exam's own `source_wording` is quoted where the research captured it. |
| **WEB-005** | Live capture does not imply "no upload" | Catalogue: 16 of 39 exams set the flag and still require an uploaded photograph | CONFIRMED | **Implemented** | `apps/web/src/lib/appearance-rules.ts` (`liveCaptureStance`), `app/exam/[examId]/page.tsx` | `exam-page-logic.test.ts` live-capture suite | Phase C | `live_capture_required` was first read as "there is no photograph to upload". RBI Assistant requires both, and the page was telling those candidates to skip the file the platform prepares and charges for — the worst failure mode inverted. Split into `additional` and `instead`. |
| **WEB-006** | Source citation reflects the evidence actually held | AGENTS.md no fictional successes | CONFIRMED | **Implemented** | `apps/web/src/components/exam/source-note.tsx`, `lib/catalogue.server.ts` | Rendered HTML: CAT reports "secondary source only / provisional" | Phase C | 7 of 39 examinations rest on secondary references, one a forum reproduction of the notification. A blanket "official source" badge would be a false claim about evidence on a page whose whole value is that its numbers can be trusted. Pairs with a correction invitation, which is the only mechanism that turns an interim estimate into a researched value. |
| **WEB-007** | Routine normalisation is not presented as a caveat | Live run against the engine | CONFIRMED | **Implemented** | `apps/web/src/components/exam/outcome-result.tsx` | `requirement-upload.test.tsx` routine-notes suite | Phase C | The API reports any non-empty `findings` as `prepared_with_findings`, so an ordinary JPEG returns the caveat state carrying `INPUT_METADATA_REMOVED` -- an internal code, and not a caveat. Nearly every file would wear the badge, training candidates to ignore the state that genuine findings depend on. The five members of the engine's own `InputWarningCode` are demoted to a collapsed "What we changed" in plain words. Fail-safe: an unrecognised code stays a caveat. Deeper fix, not taken: classify at the API rather than in the client. |
| **RULE-010** | Published rejection conditions reach the requirement they concern | Product owner request; deliverables research | CONFIRMED | **Implemented** | `scripts/encode_exam_rules.py`, `packages/exam-rules/schema/exam-rule.schema.json`, `models/exam_rule.py`, `apps/web/src/components/exam/requirement-card.tsx` | `services/image-engine/tests/test_rejection_routing.py` (23 tests) | Phase C | DEC-059: the research holds 204 conditions per examination; the schema holds them per requirement. Routed by the deliverable each names, multi-target conditions attached to every type they name, one application-wide condition carried on a new root field, and the report's "no condition was established" null-statement dropped rather than stored as an assertion. 84 of 155 requirements now carry them. Word boundaries are tested because "capital letters" contains "cap". |
| **DISC-001** | Interim placeholder disclosure, proportionate to the reader | Product owner ruling; DEC-048 | CONFIRMED | **Implemented (app side)** | `apps/web/src/lib/spec-format.ts`, `components/exam/requirement-card.tsx`, `components/exam/source-note.tsx` | `exam-page-logic.test.ts` estimate suite | Phase C | DEC-057: quiet "platform estimate" marker in the app, explicit naming in the package report. 86 interim values across 24 of 39 examinations, on 71 file sizes and 15 formats. Per-field, never a per-exam badge. |
| **WEB-008** | Image-first responsive upload-kit experience | Product owner selected UI option 3; DEC-064 | CONFIRMED | **Implemented (UI checkpoint)** | `apps/web/src/app/page.tsx`, `apps/web/src/components/exam/kit-workspace.tsx`, `requirement-panel.tsx`, `visual-guide.tsx`, `preparation-loader.tsx`, `report-issue.tsx`, `globals.css` | `exam-search.test.tsx`, `requirement-upload.test.tsx`; browser checks at 390×844 and 1440×1024 | Phase C | Desktop and mobile use intentionally different compositions. Search resolves abbreviations and full names. Guidance is visual and textual, sources and corrections remain discoverable, pricing is visible, and clean outputs never appear as unpaid previews. Payment, delivery, connected reporting and the requested 30-minute retention remain integration gates rather than simulated successes. |
| **PAY-001** | The clean file is not served before payment | Product-direction report pricing strategy; Codex's declared `preview_url`/`preview_watermarked` contract | CONFIRMED | **Implemented (release seam pending payment)** | `services/image-engine/src/exam_photo/preview/`, `api/service.py`, `api/app.py`, `api/contracts.py`, `api/jobs.py`, `api/settings.py` | `tests/preview/test_watermark.py`, `tests/api/test_purchase_gate.py` | Phase C | DEC-063: the service served neither preview field, so `/v1/jobs/{id}/output` handed the finished portal-ready file to anyone holding a job id -- and the job id is in the preparation response. The watermark is burned into a re-encoded downscale server-side, because a browser-side mark is drawn on an image the browser already holds. `/output` and the package download answer 402 until `release_job` is called; there is deliberately no HTTP route that calls it, so the honest state until Razorpay lands is prepared-and-previewed but not deliverable. A PDF gets no preview and says so rather than claiming a mark it did not apply. |
| **OPS-001** | The service is deployable: warms itself, sweeps itself, and reports which half it is in | Pre-deployment review, 2026-09-06 | CONFIRMED | **Implemented** | `services/image-engine/src/exam_photo/api/app.py`, `api/service.py`, `api/settings.py`, `orchestration/rule_pipeline.py` | `tests/api/test_service_hardening.py` (24 tests) | Phase C | DEC-064: `/health` returned `healthy` while the model was 100-150 s away, so a load balancer routed the first candidate of every deploy into a cold worker that nginx (60 s) or Cloudflare (100 s) would cut. A lifespan hook now warms on a background thread -- measured 48.8 s for the first inference against 7.7 s for the second -- and `GET /ready` reports `warming`/`ready`/`failed` with 503 on the first and last. `expires_at` was written on every manifest and acted on by nothing, since `POST /v1/cleanup-expired` had no caller; a sweeper thread now runs it every 300 s. CORS origins are configured, not hardcoded to localhost. An operator token gates `/v1/process`, `/v1/rules/validate`, `/v1/cleanup-expired` and `/test`; the candidate surface stays open because a browser cannot hold a secret. **No per-IP rate limit by design** -- CGNAT puts thousands of candidates behind one address -- so the CPU is protected by a global concurrency cap returning 429 with `Retry-After`, and the document endpoint now caps file count as well as file size. |
| **OPS-002** | The service has deployment machinery, sized on measurement | Product owner ruling: one small VPS now, GPU only if scale demands it | CONFIRMED | **Implemented (images not yet built end to end)** | `deploy/Dockerfile`, `deploy/web.Dockerfile`, `deploy/docker-compose.yml`, `deploy/Caddyfile`, `deploy/.env.example`, `deploy/README.md`, `.dockerignore` | Compose renders under `docker compose config`; a build has not been run (no Docker daemon on this machine) | Phase C | DEC-065: DEC-064's `/ready` was an endpoint nothing read. Two Dockerfile targets keep torch out of the serving image, which is the point of the ONNX export (DEC-054); weights live in a named volume because ~2.5 GB that never change should not be re-pulled every deploy. Caddy gives one origin, so CORS has nothing to permit, and the probes are answered only from private ranges because `/ready` reports the backend, the gate state and the operator-surface state. **Sizing is measured, not estimated: a warm worker holds ~2.4 GB resident, peaking ~2.9 GB, so a 2 GB VPS cannot run this and workers cost 2.4 GB each.** The web image reproduces the repo layout because `catalogue.server.ts` resolves `process.cwd()/../../examples/rules`, and the build asserts exam pages exist rather than shipping an empty site. |
| **RULE-011** | A research delivery is merged into the corpus, never adopted over it | Six research deliveries, 2026-09-06/07 | CONFIRMED | **Implemented** | `scripts/merge_research_delivery.py`, `packages/exam-rules/research/overlay_official_2026_09_07.json`, `examples/rules/` | `tests/orchestration/test_rule_catalogue.py` (13 tests, catalogue figures), encoder run 52 written / 0 rejected | Phase C | DEC-068: the newest delivery carried 117 records but zero rejection conditions against 204, and would have removed five served examinations by correctly declining to promote secondary values. Merged additively instead: a `not_found` never displaces an established value, conditions are unioned, deliverables union on a punctuation-insensitive name with the deeper entry winning. Catalogue 39 -> 52 examinations, 155 -> 215 requirements, 204 -> 276 rejection conditions, 86 -> 63 interim placeholders. **BPSC withdrawn**: its official portal instructions, in Krutidev-encoded Hindi, say the candidate is photographed through a web camera, so the catalogue described an upload that does not exist. |
